<h1> Hello</h1>
<p> If you were able to access this page, that means session[:user_name] is not empty. i.e. User name is stored in session.

<%= link_to('Logout',access_logout_path, {class: 'btn btn-warning'}) %>